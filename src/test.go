package main

// import "taz/modules/macrocontroller"

// func main() {
// 	mc := macrocontroller.NewController()
// 	mc.Engine.Start(mc.Macro)
// 	select {}

// 	// robloxPid, _ := windowmanager.GetRobloxPID()
// 	// windowmanager.SetWindowBounds(robloxPid, 0, 0, 0, 0)
// 	// time.Sleep(2 * time.Second)
// 	// x, y, w, h := windowmanager.GetRobloxWindowBounds()
// 	// needle := bitmap.Open("images/test.png")
// 	// defer robotgo.FreeBitmap(needle)

// 	// screen := robotgo.CaptureScreen(x, y, w, h)
// 	// defer robotgo.FreeBitmap(screen)

// 	// fx, fy := bitmap.Find(needle, screen)
// 	// println(fx, fy)

// 	// img := robotgo.ToImage(screen)
// 	// imgo.Save("test2.png", img)
// }
